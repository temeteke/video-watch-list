<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.videowatchlog.infrastructure.persistence.EpisodeMapper">

    <resultMap id="episodeMap" type="com.example.videowatchlog.domain.model.Episode">
        <id property="id" column="id"/>
        <result property="seriesId" column="series_id"/>
        <result property="episodeInfo" column="episode_info"/>
        <result property="watchStatus" column="watch_status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <collection property="watchPageUrls" ofType="com.example.videowatchlog.domain.model.WatchPageUrl"
                    select="selectWatchPageUrlsByEpisodeId" column="id"/>
        <collection property="viewingRecords" ofType="com.example.videowatchlog.domain.model.ViewingRecord"
                    select="selectViewingRecordsByEpisodeId" column="id"/>
    </resultMap>

    <select id="findById" resultMap="episodeMap">
        SELECT id, series_id, episode_info, watch_status, created_at, updated_at
        FROM episodes
        WHERE id = #{id}
    </select>

    <select id="findBySeriesId" resultMap="episodeMap">
        SELECT id, series_id, episode_info, watch_status, created_at, updated_at
        FROM episodes
        WHERE series_id = #{seriesId}
        ORDER BY id
    </select>

    <insert id="insert" parameterType="com.example.videowatchlog.domain.model.Episode"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO episodes (series_id, episode_info, watch_status, created_at, updated_at)
        VALUES (#{seriesId}, #{episodeInfo}, #{watchStatus}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="update" parameterType="com.example.videowatchlog.domain.model.Episode">
        UPDATE episodes
        SET episode_info = #{episodeInfo},
            watch_status = #{watchStatus},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM episodes WHERE id = #{id}
    </delete>

    <select id="selectWatchPageUrlsByEpisodeId" resultType="com.example.videowatchlog.domain.model.WatchPageUrl">
        SELECT url, platform
        FROM watch_page_urls
        WHERE episode_id = #{id}
        ORDER BY id
    </select>

    <select id="selectViewingRecordsByEpisodeId" resultType="com.example.videowatchlog.domain.model.ViewingRecord">
        SELECT id, episode_id, watched_at, rating, comment, created_at as recorded_at
        FROM viewing_records
        WHERE episode_id = #{id}
        ORDER BY recorded_at DESC
    </select>

</mapper>
