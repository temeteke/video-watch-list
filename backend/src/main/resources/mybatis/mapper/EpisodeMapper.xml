<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.videowatchlog.infrastructure.persistence.EpisodeMapper">

    <resultMap id="episodeEntityMap" type="com.example.videowatchlog.infrastructure.persistence.entity.EpisodeEntity">
        <id property="id" column="id"/>
        <result property="seriesId" column="series_id"/>
        <result property="episodeInfo" column="episode_info"/>
        <result property="watchStatus" column="watch_status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findById" resultMap="episodeEntityMap">
        SELECT id, series_id, episode_info, watch_status, created_at, updated_at
        FROM episodes
        WHERE id = #{id}
    </select>

    <select id="findBySeriesId" resultMap="episodeEntityMap">
        SELECT id, series_id, episode_info, watch_status, created_at, updated_at
        FROM episodes
        WHERE series_id = #{seriesId}
        ORDER BY id
    </select>

    <insert id="insert" parameterType="com.example.videowatchlog.infrastructure.persistence.entity.EpisodeEntity"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO episodes (series_id, episode_info, watch_status, created_at, updated_at)
        VALUES (#{seriesId}, #{episodeInfo}, CAST(#{watchStatus} AS watch_status), #{createdAt}, #{updatedAt})
    </insert>

    <update id="update" parameterType="com.example.videowatchlog.infrastructure.persistence.entity.EpisodeEntity">
        UPDATE episodes
        SET episode_info = #{episodeInfo},
            watch_status = CAST(#{watchStatus} AS watch_status),
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM episodes WHERE id = #{id}
    </delete>

    <select id="selectWatchPageUrlsByEpisodeId" resultType="com.example.videowatchlog.domain.model.WatchPageUrl">
        SELECT url
        FROM watch_page_urls
        WHERE episode_id = #{id}
        ORDER BY id
    </select>

    <select id="selectViewingRecordsByEpisodeId" resultType="com.example.videowatchlog.domain.model.ViewingRecord">
        SELECT id, episode_id, watched_at, rating, comment, recorded_at
        FROM viewing_records
        WHERE episode_id = #{id}
        ORDER BY recorded_at DESC
    </select>

    <insert id="insertWatchPageUrl">
        INSERT INTO watch_page_urls (episode_id, url)
        VALUES (#{episodeId}, #{url})
    </insert>

    <delete id="deleteWatchPageUrlsByEpisodeId">
        DELETE FROM watch_page_urls WHERE episode_id = #{episodeId}
    </delete>

</mapper>
