<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.videowatchlog.infrastructure.persistence.ViewingRecordMapper">

    <resultMap id="viewingRecordMap" type="com.example.videowatchlog.domain.model.ViewingRecord">
        <id property="id" column="id"/>
        <result property="episodeId" column="episode_id"/>
        <result property="watchedAt" column="watched_at"/>
        <result property="rating" column="rating"/>
        <result property="comment" column="comment"/>
        <result property="recordedAt" column="recorded_at"/>
    </resultMap>

    <select id="findById" resultMap="viewingRecordMap">
        SELECT id, episode_id, watched_at, rating, comment, created_at as recorded_at
        FROM viewing_records
        WHERE id = #{id}
    </select>

    <select id="findByEpisodeId" resultMap="viewingRecordMap">
        SELECT id, episode_id, watched_at, rating, comment, created_at as recorded_at
        FROM viewing_records
        WHERE episode_id = #{episodeId}
        ORDER BY recorded_at DESC
    </select>

    <insert id="insert" parameterType="com.example.videowatchlog.domain.model.ViewingRecord"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO viewing_records (episode_id, watched_at, rating, comment, created_at)
        VALUES (#{episodeId}, #{watchedAt}, #{rating}, #{comment}, #{recordedAt})
    </insert>

    <delete id="delete">
        DELETE FROM viewing_records WHERE id = #{id}
    </delete>

</mapper>
